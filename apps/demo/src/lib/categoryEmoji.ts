import type { IngredientCategory } from '@/types/domain';

export const categoryEmoji: Record<IngredientCategory, string> = {
  viande: '🥩',
  poisson: '🐟',
  cereales: '🌾',
  boisson: '🍺',
  herbe: '🌿',
  laitier: '🧀',
  autre: '📦',
};
